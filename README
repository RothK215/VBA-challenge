So the data given was over 25mb which GitHub doesnt allow. So I uploaded the VBA I used for the calculations and the final results I got from using the VBA.
Also, the bonus is also in the VBA file but may require a second use for the macro. I couldn't figure out how to shorten the script to ensure one button use.
